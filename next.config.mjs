/** @type {import('next').NextConfig} */
const nextConfig = {
    pageExtensions: ['ts', 'tsx'],
    images: {
        domains: ['fotosretiroanas2022.s3.us-east-2.amazonaws.com'],
      }
};

export default nextConfig;
